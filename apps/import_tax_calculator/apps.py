"""App configuration for import_tax_calculator."""

from django.apps import AppConfig


class ImportTaxCalculatorConfig(AppConfig):
    """Configuration for the import_tax_calculator app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.import_tax_calculator"
